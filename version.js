// Single source of truth for the app version.
// Bumped automatically on every push by bump.js.
// Works in both the page (self === window) and the service worker (self === SW global).
self.APP_VERSION = "4.76.0";
